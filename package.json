{
  "name": "qase-postman-demo",
  "private": true,
  "scripts": {
    "test": "newman run postman/collection.json -e postman/env.json -r qase"
  },
  "devDependencies": {
    "newman": "^6.0.0",
    "newman-reporter-qase": "^1.0.0"
  }
}
